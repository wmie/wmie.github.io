# Publishes the site as a Pages artifact: no commit lands on main, so the org ruleset
# (pull request + status checks on main) only applies to changes of the code itself.
name: Pages

on:
  push:
    branches: [main]
  schedule:
    - cron: '17 */3 * * *'   # every three hours: picks up new items on wmie.it/design
  repository_dispatch:
    types: [playground-updated]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node scripts/playground.mjs
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deploy
        uses: actions/deploy-pages@v4
